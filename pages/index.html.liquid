<html>

<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ project_name }} Repositories</title>
	<link rel="stylesheet" href="main.css">
</head>

<body>
	<div class="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50">
		<div class="max-w-4xl mx-auto px-6 py-12">
			<!-- Header -->
			<div class="text-center mb-12">
				<h1 class="text-4xl font-bold text-slate-800 mb-4">
					<a href="{{ project_url }}">{{ project_name }}</a>
				</h1>
			</div>

			<!-- Debian -->
			<section class="mb-12">
				<div class="bg-white rounded-xl shadow-lg p-8 border border-slate-200">
					<h2 class="text-2xl font-semibold text-slate-800 mb-6 flex items-center">
						<span class="w-8 h-8 bg-red-500 rounded-lg flex items-center justify-center text-white text-sm font-bold mr-3">D</span>
						Install on Debian (.deb)
					</h2>

					<div class="mb-6">
						<div class="flex flex-wrap gap-2">
							{% assign architectures = repo_arch_deb | split: " " %}
							{% for architecture in architectures %}
							<span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-medium">{{ architecture }}</span>
							{% endfor %}
						</div>
					</div>

					<div class="mb-6">
						<h3 class="text-lg font-semibold text-slate-700 mb-4 pb-1 border-l-4 border-amber-400 pl-3">GPG Key</h3>
						<p class="text-slate-600 mb-3">
							Add the GPG key of the repository:
						</p>
						<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
							<pre><code class="text-code text-sm">curl -fsSL https://repo.vscodium.dev/vscodium.gpg \
  | gpg --dearmor \
  | sudo dd of=/usr/share/keyrings/vscodium.gpg</code></pre>
						</div>
					</div>

					<div class="mb-6">
						<h3 class="text-lg font-semibold text-slate-700 mb-4 pb-1 border-l-4 border-purple-400 pl-3">Repository</h3>
						<div class="space-y-6">
							<div class="border-l-3 border-orange-400 bg-orange-50 rounded-r-lg p-6">
								<div class="flex items-center mb-3">
									<span class="px-3 py-1 bg-orange-500 text-white rounded-full text-sm font-semibold mr-3">v13</span>
									<h4 class="text-lg font-semibold text-orange-800">Debian 13 / Ubuntu 24.04 or newer</h4>
								</div>
								<p class="text-orange-700 mb-3">
									Add the <code class="bg-orange-100 px-2 py-1 rounded text-sm">.sources</code> file:
								</p>
								<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
									<pre><code class="text-code text-sm">sudo curl --output-dir /etc/apt/sources.list.d -LO {{ repo_url }}/{{ repo_name }}.sources</code></pre>
								</div>
							</div>
							<div class="border-l-3 border-emerald-400 bg-emerald-50 rounded-r-lg p-6">
								<div class="flex items-center mb-3">
									<span class="px-3 py-1 bg-emerald-500 text-white rounded-full text-sm font-semibold mr-3">v12</span>
									<h4 class="text-lg font-semibold text-emerald-800">Debian 12 / Ubuntu 23.10 or older</h4>
								</div>
								<p class="text-emerald-700 mb-3">
									Add the <code class="bg-emerald-100 px-2 py-1 rounded text-sm">.list</code> file:
								</p>
								<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
									<pre><code class="text-code text-sm">sudo curl --output-dir /etc/apt/sources.list.d -LO {{ repo_url }}/{{ repo_name }}.list</code></pre>
								</div>
							</div>
						</div>
					</div>

					<div class="mb-6">
						<h3 class="text-lg font-semibold text-slate-700 mb-4 pb-1 border-l-4 border-green-400 pl-3">Install</h3>
						<p class="text-slate-600 mb-3">Update then install {{ project_name }}:</p>
              			<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
                			<pre><code class="text-code text-sm">sudo apt update && sudo apt install {{ package_name }}</code></pre>
              			</div>
					</div>
				</div>
			</section>

			<!-- Fedora -->
			<section class="mb-12">
				<div class="bg-white rounded-xl shadow-lg p-8 border border-slate-200">
					<h2 class="text-2xl font-semibold text-slate-800 mb-6 flex items-center">
						<span class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center text-white text-sm font-bold mr-3">F</span>
						Install Fedora/Suse (.rpm)
					</h2>

					<div class="mb-6">
						<div class="flex flex-wrap gap-2">
							{% assign architectures = repo_arch_rpm | split: " " %}
							{% for architecture in architectures %}
							<span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-medium">{{ architecture }}</span>
							{% endfor %}
						</div>
					</div>

					<div class="mb-6">
						<h3 class="text-lg font-semibold text-slate-700 mb-4 pb-1 border-l-4 border-purple-400 pl-3">Repository</h3>
						<p class="text-slate-700 mb-3">
							Add the <code class="bg-slate-100 px-2 py-1 rounded text-sm">.repo</code> file:
						</p>
						<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
							<pre><code class="text-code text-sm">sudo curl --output-dir /etc/yum.repos.d -LO {{ repo_url }}/{{ repo_name }}.repo</code></pre>
						</div>
					</div>

					<div class="mb-6">
						<h3 class="text-lg font-semibold text-slate-700 mb-4 pb-1 border-l-4 border-green-400 pl-3">Install</h3>
						<div class="space-y-6">
							<div class="border-l-3 border-orange-400 bg-orange-50 rounded-r-lg p-6">
								<div class="flex items-center mb-3">
									<span class="px-3 py-1 bg-orange-500 text-white rounded-full text-sm font-semibold mr-3">F</span>
									<h4 class="text-lg font-semibold text-orange-800">Fedora/RHEL</h4>
								</div>
								<p class="text-orange-700 mb-3">
									Install {{ project_name }}:
								</p>
								<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
									<pre><code class="text-code text-sm">sudo dnf install {{ package_name }}</code></pre>
								</div>
							</div>
							<div class="border-l-3 border-emerald-400 bg-emerald-50 rounded-r-lg p-6">
								<div class="flex items-center mb-3">
									<span class="px-3 py-1 bg-emerald-500 text-white rounded-full text-sm font-semibold mr-3">S</span>
									<h4 class="text-lg font-semibold text-emerald-800">OpenSuse/Suse</h4>
								</div>
								<p class="text-emerald-700 mb-3">
									Install {{ project_name }}:
								</p>
								<div class="bg-slate-900 rounded-lg p-4 overflow-x-auto">
									<pre><code class="text-code text-sm">sudo zypper in {{ package_name }}</code></pre>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- Packages -->
			<section>
          		<div class="bg-white rounded-xl shadow-lg p-8 border border-slate-200">
            		<h2 class="text-2xl font-semibold text-slate-800 mb-6 flex items-center">
              			<span class="w-8 h-8 bg-green-600 rounded-lg flex items-center justify-center text-white text-sm font-bold mr-3">📦</span>
              			Available Packages
            		</h2>

            		<div class="grid grid-cols-1 gap-1">
						{% for package in packages %}
						<div class="bg-slate-50 rounded-lg p-1 border border-slate-200 hover:bg-slate-100 transition-colors">
                  			<code class="text-sm text-slate-700 break-all">{{ package }}</code>
                		</div>
						{% endfor %}
					</div>
				</div>
			</section>
		</div>
	</div>
</body>

</html>
